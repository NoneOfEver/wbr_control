# T2: Star topology, application is the manifest repository.
# wbr_control is the manifest repository (application-as-manifest).
#
# West workspace configuration (.west/config):
#   [manifest]
#   path = wbr_control
#   file = west.yml

########################################
##  zephyr                    v3.7.0  ##
##  hpm_sdk                   v1.11.0 ##
########################################
manifest:
  version: "1.0"

  remotes:
    - name: github
      url-base: https://github.com/hpmicro
    - name: cherry-embedded
      url-base: https://github.com/cherry-embedded/
    - name: cherryusb-fork
      url-base: https://github.com/NoneOfEver
    - name: upstream
      url-base: https://github.com/zephyrproject-rtos
    - name: dust-mavlink
      url-base: https://github.com/NoneOfEver
    - name: dust-hpm
      url-base: https://github.com/NoneOfEver
    # Eigen 官方仓库在 GitLab（GitHub 的 eigenteam/eigen-git-mirror 已停更在 3.3.7）。
    - name: eigen
      url-base: https://gitlab.com
    - name: sophus
      url-base: https://github.com/strasdat
    - name: tinympc
      url-base: https://github.com/NoneOfEver

  defaults:
    remote: github

  projects:
    # Zephyr RTOS. Third-party projects are declared explicitly below so every
    # repository can live directly under the workspace root.
    - name: zephyr
      remote: github
      repo-path: zephyr
      revision: zsg_v0.7.0
      west-commands: scripts/west-commands.yml

    # ELM FatFs implementation required by Zephyr's FAT filesystem layer.
    - name: fatfs
      remote: upstream
      repo-path: fatfs
      path: fatfs
      revision: 427159bf95ea49b7680facffaa29ad506b42709b

    # HPMicro SDK (manifest repo with boards, drivers, soc, dts)
    - name: hpm_support
      remote: dust-hpm
      repo-path: hpm_support
      path: hpm_support
      revision: main
      west-commands: scripts/west-commands.yml

    # SEGGER RTT transport used by the non-UART shell/logging diagnostics.
    # Keep this revision aligned with the Zephyr 3.7 manifest.
    - name: segger
      remote: upstream
      repo-path: segger
      path: segger
      revision: b011c45b585e097d95d9cf93edf4f2e01588d3cd

    - name: CherryUSB
      remote: cherryusb-fork
      repo-path: CherryUSB
      path: CherryUSB
      revision: 50c7e9085801e24d2d0991d0c4c1609fdbcfb90a

    # Mavlink generated headers
    - name: dust_mavlink
      remote: dust-mavlink
      repo-path: dust_mavlink
      path: mavlink
      revision: main

    # Eigen：纯头文件线性代数库（MPL-2.0），用于状态估计 / MPC / 坐标系变换。
    # 3.4.0 为当前最新稳定版，对应 commit 3147391d946bb4b6c68edd901f2add6ac1f31f8c。
    - name: eigen
      remote: eigen
      repo-path: libeigen/eigen
      path: eigen
      revision: 3.4.0

    # Sophus：基于 Eigen 的纯头文件李群库。固件仅使用固定尺寸 SO3f，禁止在
    # 1 kHz 实时路径中使用字符串 frame、动态缓存或失败格式化路径。
    - name: sophus
      remote: sophus
      repo-path: Sophus
      path: sophus
      revision: d0b7315a0d90fc6143defa54596a3a95d9fa10ec

    # TinyMPC：面向资源受限 MCU 的 MPC 求解器。目前只供独立 host test
    # 使用，尚未链接到正式 Zephyr 固件目标。
    - name: TinyMPC
      remote: tinympc
      repo-path: TinyMPC
      path: TinyMPC
      revision: 023f36bd5b27267e1a96ed47a774d120b5707f13

  self:
    path: wbr_control
    west-commands: scripts/west-commands.yml
